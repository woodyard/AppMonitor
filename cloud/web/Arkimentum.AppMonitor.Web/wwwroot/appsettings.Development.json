{
  "//": "Local development against `func start` in cloud/api. The API must allow https://localhost:7200 in its CORS settings, and the Entra app registration needs https://localhost:7200/authentication/login-callback as a SPA redirect URI.",
  "ApiBaseUrl": "http://localhost:7071"
}
