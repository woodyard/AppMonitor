<Window x:Class="Arkimentum.AppMonitor.Admin.Views.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Icon="pack://application:,,,/Arkimentum.AppMonitor.UI;component/Assets/app.ico"
        FontFamily="{StaticResource AppFontFamily}"
        Foreground="{DynamicResource BrandInkBrush}"
        Background="{DynamicResource BrandBackgroundBrush}">

    <StackPanel Margin="24,20,24,20">
        <TextBlock Style="{StaticResource SubtitleText}" Text="{Binding Title}" />
        <TextBlock Style="{StaticResource CaptionText}" Margin="0,8,0,0" Text="{Binding Prompt}" />

        <TextBox x:Name="Input"
                 Margin="0,12,0,0"
                 Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AutomationProperties.Name="{Binding Prompt}" />

        <TextBlock Style="{StaticResource ErrorText}"
                   Margin="0,8,0,0"
                   Text="{Binding Error}"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button MinWidth="104"
                    Padding="16,8"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Content="{Binding CancelText}"
                    Command="{Binding CancelCommand}" />
            <Button MinWidth="104"
                    Padding="16,8"
                    IsDefault="True"
                    Style="{DynamicResource AccentButtonStyle}"
                    Content="{Binding AcceptText}"
                    Command="{Binding AcceptCommand}" />
        </StackPanel>
    </StackPanel>
</Window>
